//counter
var button = document.getElementByID('counter');
button.onclick= function() {
    
    // make a request to counter end point
    
    // capture the rsponse and sotre it in a variable
     // render the variable
     counter = counter + 1;
     
}